#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 *
 */

int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)

for (alpha = 'A'; alpha <= 'Z'; alpha++)
putchar(alpha);
putchar('\n');

return (0);
}
