#include <stdio.h>
#include <string.h>
/**
 * main - entry point
 * return: always 0
 *
 */

int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
putchar(alpha);
putchar('\n');

return (0);
}
